{
  "name": "figma-ui-agent-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:set": "echo '设置 API Key:' && wrangler secret put ARK_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.3.3",
    "wrangler": "^4.83.0"
  }
}
