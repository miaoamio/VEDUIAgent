# Debug Session [OPEN]

## 症状
- 表单字段切换到日期/时间/多行文本时高度变为 1px（应为组件原始高度/注册表默认高度）。

## 期望
- 高度保持组件原始高度，宽度填充容器。

## 复现步骤
1. 在表单字段中切换控件类型为日期选择/时间选择/多行文本。
2. 观察控件高度显示为 1px。

## 环境
- macOS / Figma 插件 (figmaUIagent)

## 假设
- H1：控件实例在插入后被某个布局函数重新 resize 到 1px。
- H2：replaceSceneNode 后布局继承导致高度被折叠为 1px。
- H3：控件被包裹在容器中，容器高度/对齐设置导致子节点塌陷为 1px。
- H4：渲染流程中记录的高度为 0/1，后续按该值强制 resize。
- H5：Figma 组件实例未完成加载时读取高度为 1，后续未刷新。

## 进展记录
- [x] 启动 Debug Server 收集运行时日志
- [x] 插桩记录控件创建/替换关键高度与布局属性
- [x] 复现并收集日志
- [ ] 补充 post-layout 插桩并复现
- [ ] 监听尺寸变更并定位 1px 来源
- [ ] 基于证据修复并验证
